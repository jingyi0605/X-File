# 文档库视图迁移测试进度

本文档记录 X-File 按批次通过测试迁移 CodingNS 文档库视图功能的进度。每正式完成一个批次，需要同步更新本文档。

## 当前结论

- 已建立前端 Vitest 测试栈和 `LibraryPage` mock API 层。
- 已完成第 1 批“文档预览、编辑写回、Office 阅读视图与目录详情”的测试和功能补全。
- 已完成第 2 批“文件夹打开行为、目录选中与目录导航”的测试和功能补全。
- 已完成第 3 批“macOS 原生右键菜单与桌面本地动作”的测试和功能补全。
- 已完成第 4 批“H5/Web 右键菜单、文件操作与剪贴板”的测试和功能补全。
- 已完成第 5 批“文档库设置、启用状态与侧栏状态”的测试和功能补全。
- 已完成第 6 批“索引状态、刷新策略、轮询、缓存替换与索引稳定性优化”的测试和功能补全。
- 已完成第 7 批“列表/Finder 视图、文件类型文案、列宽、虚拟滚动与加载更多”的测试和功能补全。
- 已完成第 8 批“标签筛选树、多选过滤、徽标计数、排序、展开记忆与拼音搜索”的测试和功能补全。
- 已完成第 9 批“标签管理、智能规则与全量重算任务”的测试和功能补全。
- 已完成第 10 批“文档与文件夹标签分配、推荐标签和任务进度入口”的测试和功能补全。
- 已完成第 11 批“跨批基础合约、DTO、路由、索引器与样式体系”的测试和功能补全。
- 第 1 批对应 4 个行为项均已有自动化测试证据。
- 第 2 批对应 7 个行为项均已有自动化测试证据。
- 第 3 批对应 7 个行为项均已有自动化测试证据。
- 第 4 批对应 6 个行为项均已有自动化测试证据。
- 第 5 批对应 5 个行为项均已有自动化测试证据。
- 第 6 批对应 11 个行为项均已有自动化测试证据或后端稳定性测试证据。
- 第 7 批对应 12 个行为项均已有自动化测试证据。
- 第 8 批对应 11 个行为项均已有自动化测试证据。
- 第 9 批对应 7 个行为项均已有自动化测试证据。
- 第 10 批对应 8 个行为项均已有自动化测试证据。
- 第 11 批对应基础合约、DTO、路由、索引器与样式体系的跨批验收已补充测试。
- 后端第 11 批中与第 6 批强相关的索引稳定性 P0 项已补充测试：任务运行超时、失败恢复、cooldown 恢复、running 只读列表。

## 已完成批次

### 第 1 批：文档预览、编辑写回、Office 阅读视图与目录详情

状态：已完成。

#### 行为项覆盖

| 序号 | 旧测试行号 | 行为规格 | X-File 测试证据 | 功能补全 |
|---:|---:|---|---|---|
| 1 | 1974 | 双击文档库文档会复用文件预览工具并走文档库默认预览接口 | `apps/web/src/features/library/__tests__/LibraryPreviewMigration.test.tsx` | 双击正式预览调用 `getLibraryPreview(path)`，不带 reading 模式，继续打开文件预览弹窗 |
| 2 | 1997 | 文档库预览编辑后会走文档库写回接口保存 | `LibraryPreviewMigration.test.tsx` | 预览编辑保存调用 `operateLibraryFile({ opType: "write", srcPath, content, expectedVersion })`，`expectedVersion` 来自预览版本 |
| 3 | 2020 | 目录详情标题会居中显示，并复用和文档详情一致的摘要折叠逻辑 | `LibraryPreviewMigration.test.tsx` | `LibraryDetail` 增加目录详情分支，目录标题、路径、更新时间、类型和文档数量进入右侧详情；文档与目录共用 `LibraryDetailSummary` 折叠逻辑 |
| 4 | 2068 | 右侧对象详情栏底部的 Office 预览会走阅读视图，不影响双击正式预览 | `LibraryPreviewMigration.test.tsx` | 右侧详情栏预览调用 `getLibraryPreview(path, "reading")`；双击正式预览仍调用 `getLibraryPreview(path)` |

#### 本批新增/修改的关键文件

- `apps/web/src/features/library/__tests__/LibraryPreviewMigration.test.tsx`
- `apps/web/src/features/library/LibraryPage.tsx`
- `apps/web/src/features/library/useLibraryState.ts`
- `apps/web/src/i18n.ts`
- `apps/web/src/styles.css`

#### 已执行验证

```bash
pnpm --filter @x-file/web test -- src/features/library/__tests__/LibraryPreviewMigration.test.tsx
pnpm --filter @x-file/web test
pnpm --filter @x-file/web typecheck
pnpm --filter @x-file/server test
pnpm --filter @x-file/server typecheck
pnpm --filter @x-file/shared build
pnpm --filter @x-file/indexer build
```

结果：全部通过。前端 8 个测试文件、49 个测试通过；后端 33 个测试通过。

### 第 2 批：文件夹打开行为、目录选中与目录导航

状态：已完成。

#### 行为项覆盖

| 序号 | 旧测试行号 | 行为规格 | X-File 测试证据 | 功能补全 |
|---:|---:|---|---|---|
| 1 | 2031 | 文档库文件夹默认双击进入，单击只选中并显示目录详情 | `apps/web/src/features/library/__tests__/LibraryFolderNavigationMigration.test.tsx` | 默认 `double_click` 下单击只设置 `selectedFolderEntryPath`，右侧显示目录详情；双击才调用 `selectFolder(path)` 进入目录 |
| 2 | 2051 | 单击选中文件夹时，当前目录下的文件不会消失 | `LibraryFolderNavigationMigration.test.tsx` | 单击目录不改 `selectedFolderPath`，当前目录文档列表保持可见，不触发子目录列表请求 |
| 3 | 4041 | 点击根路径按钮时会切回文件夹根目录 | `LibraryFolderNavigationMigration.test.tsx` | 面包屑根按钮调用 `selectFolder(null)`，切回 folder 根目录并重拉根目录列表 |
| 4 | 4064 | 返回上级后会保留来源文件夹高亮 | `LibraryFolderNavigationMigration.test.tsx` | `selectFolder(path, selectedEntryPath)` 支持导航后保留来源目录高亮；面包屑父级点击传入下一层路径作为高亮项 |
| 5 | 5780 | 文档库设置可以切换文件夹单击/双击打开方式 | `apps/web/src/features/settings/__tests__/SettingsPage.test.tsx` | 设置页开关保存 `folderOpenBehavior: "single_click"` 到文档库配置 payload |
| 6 | 5794 | 切到单击打开后，单击文件夹会直接进入目录 | `LibraryFolderNavigationMigration.test.tsx`、`apps/web/src/features/library/__tests__/LibraryPage.test.tsx` | `handleFolderClick()` 根据 binding 中的 `folderOpenBehavior` 决定单击进入或只选中 |
| 7 | 5931 | 详情区文档路径支持逐级点击跳转 | `LibraryFolderNavigationMigration.test.tsx` | 文档详情路径改为分段按钮；点击中间目录段调用 `selectFolder(targetPath)` 并加载目标目录 |

#### 本批新增/修改的关键文件

- `apps/web/src/features/library/__tests__/LibraryFolderNavigationMigration.test.tsx`
- `apps/web/src/features/settings/__tests__/SettingsPage.test.tsx`
- `apps/web/src/features/library/LibraryPage.tsx`
- `apps/web/src/features/library/useLibraryState.ts`
- `apps/web/src/styles.css`

#### 已执行验证

```bash
pnpm --filter @x-file/web test -- src/features/library/__tests__/LibraryFolderNavigationMigration.test.tsx
pnpm --filter @x-file/web test -- src/features/settings/__tests__/SettingsPage.test.tsx
pnpm --filter @x-file/web test
pnpm --filter @x-file/web typecheck
pnpm --filter @x-file/server test
pnpm --filter @x-file/server typecheck
pnpm --filter @x-file/shared build
pnpm --filter @x-file/indexer build
```

结果：全部通过。前端 9 个测试文件、56 个测试通过；后端 33 个测试通过。

### 第 3 批：macOS 原生右键菜单与桌面本地动作

状态：已完成。

#### 行为项覆盖

| 序号 | 旧测试行号 | 行为规格 | X-File 测试证据 | 功能补全 |
|---:|---:|---|---|---|
| 1 | 2280 | macOS 桌面端会优先使用原生右键菜单 | `apps/web/src/features/library/__tests__/LibraryDesktopContextMigration.test.tsx` | macOS desktop 平台右键优先调用 Tauri `show_library_context_menu`，不渲染 Web fallback DOM 菜单 |
| 2 | 2299 | macOS 原生右键菜单点击删除后会先打开确认弹窗 | `LibraryDesktopContextMigration.test.tsx` | 原生菜单返回 `delete` 时只设置删除确认状态，确认前不调用 `operateLibraryFile` |
| 3 | 2321 | 桌面端空白处右键菜单会包含新建、刷新、粘贴和属性 | `LibraryDesktopContextMigration.test.tsx` | 原生空白菜单补齐 `new-group`、`refresh`、`paste`、`properties`；新建子项可打开 X-File 新建弹窗 |
| 4 | 2334 | macOS 原生右键菜单点击定位后会切到文件所在目录 | `LibraryDesktopContextMigration.test.tsx` | 原生菜单 `locate` 对文档调用父目录导航，嵌套文档会加载其父目录 |
| 5 | 2361 | macOS 原生右键菜单点击使用本地应用程序打开会走镜像路径 | `LibraryDesktopContextMigration.test.tsx` | 本地打开路径解析优先使用 `binding.mirrorRoot`，没有镜像时才退回 `rootDir` |
| 6 | 5844 | 详情区在有镜像路径时会提供本地文件动作，并显示完整元信息 | `LibraryDesktopContextMigration.test.tsx` | 文档详情显示本地镜像路径、大小、创建/更新时间，并在有 `mirrorRoot` 时显示“使用本地应用打开”按钮 |
| 7 | 5884 | 详情区在没有镜像路径时会隐藏本地文件动作 | `LibraryDesktopContextMigration.test.tsx` | `mirrorRoot` 为空时隐藏本地打开按钮和本地镜像路径信息 |

#### 本批新增/修改的关键文件

- `apps/web/src/features/library/__tests__/LibraryDesktopContextMigration.test.tsx`
- `apps/web/src/features/library/LibraryPage.tsx`
- `apps/web/src/i18n.ts`

#### 已执行验证

```bash
pnpm --filter @x-file/web test -- src/features/library/__tests__/LibraryDesktopContextMigration.test.tsx
pnpm --filter @x-file/web test
pnpm --filter @x-file/web typecheck
pnpm --filter @x-file/server test
pnpm --filter @x-file/server typecheck
pnpm --filter @x-file/shared build
pnpm --filter @x-file/indexer build
```

结果：全部通过。前端 10 个测试文件、63 个测试通过；后端 33 个测试通过。

### 第 4 批：H5/Web 右键菜单、文件操作与剪贴板

状态：已完成。

#### 行为项覆盖

| 序号 | 旧测试行号 | 行为规格 | X-File 测试证据 | 功能补全 |
|---:|---:|---|---|---|
| 1 | 2379 | 文档库文件右键菜单点击删除后会先确认再删除 | `apps/web/src/features/library/__tests__/LibraryWebContextMigration.test.tsx` | Web 右键删除先打开 `删除项目` 确认弹窗，确认前不调用删除 API |
| 2 | 2424 | H5 右键菜单点击分配标签后会打开快捷分配面板 | `LibraryWebContextMigration.test.tsx` | Web 右键“标签”复用 `LibraryTagAssignmentModal` 快捷分配弹窗 |
| 3 | 2437 | 右键分配标签面板会显示最多 8 个推荐标签，并可一键分配 | `LibraryWebContextMigration.test.tsx` | 推荐标签区域最多 8 个，点击推荐项会调用 `saveDocumentTags` 分配 |
| 4 | 2533 | H5 环境下右键下载仍然可用 | `LibraryWebContextMigration.test.tsx` | Web 右键“下载”调用 `downloadLibraryFile` 并触发浏览器下载链路 |
| 5 | 2550 | 文档库空白处右键菜单只保留可粘贴操作 | `LibraryWebContextMigration.test.tsx` | 空白处菜单只展示空白区动作：新建、粘贴、刷新、属性，不展示删除、标签、下载等文件动作 |
| 6 | 2567 | H5 空白处右键可以新建目录并刷新列表 | `LibraryWebContextMigration.test.tsx` | 空白处“新建目录”打开新建弹窗，创建成功后调用 `create_directory` 并刷新当前列表 |

#### 本批新增/修改的关键文件

- `apps/web/src/features/library/__tests__/LibraryWebContextMigration.test.tsx`
- `apps/web/src/features/library/LibraryPage.tsx`

#### 已执行验证

```bash
pnpm --filter @x-file/web test -- src/features/library/__tests__/LibraryWebContextMigration.test.tsx
pnpm --filter @x-file/web test
pnpm --filter @x-file/web typecheck
pnpm --filter @x-file/server test
pnpm --filter @x-file/server typecheck
pnpm --filter @x-file/shared build
pnpm --filter @x-file/indexer build
```

结果：全部通过。前端 11 个测试文件、69 个测试通过；后端 33 个测试通过。

### 第 5 批：文档库设置、启用状态与侧栏状态

状态：已完成。

#### 行为项覆盖

| 序号 | 旧测试行号 | 行为规格 | X-File 测试证据 | 功能补全 |
|---:|---:|---|---|---|
| 1 | 2594 | 点击设置按钮后会在独立模态框里显示文档库设置 | `apps/web/src/features/library/__tests__/LibrarySettingsStateMigration.test.tsx`、`apps/web/src/features/settings/__tests__/SettingsPage.test.tsx` | 文档库视图设置入口触发上层独立设置弹窗；设置页资料库 tab 展示并保存索引配置 |
| 2 | 2659 | 文档库左侧栏不再显示旧的说明头和浏览模式切换 | `LibrarySettingsStateMigration.test.tsx` | 左侧栏只保留 X-File 品牌、标签树、收藏入口和设置入口，不渲染旧说明头、摘要和文件夹/标签模式 tab |
| 3 | 3697 | 没有收藏内容时会自动隐藏收藏夹分组 | `LibrarySettingsStateMigration.test.tsx` | `favorites` 为空时不渲染收藏分组，也不显示收藏空态占位 |
| 4 | 3706 | 可以切换文档库启用状态 | `LibrarySettingsStateMigration.test.tsx`、`apps/server/src/library/library-config-service.test.ts` | `SaveLibraryConfigInput` 补齐 `enabled`；设置页“启用资料库”开关提交 `enabled`；后端保存到 binding 和资料库配置文件 |
| 5 | 3721 | 文档库未启用时不会请求文档和文件夹标签详情 | `LibrarySettingsStateMigration.test.tsx` | `useLibraryState.reload()` 在未启用时不拉标签；未启用视图显示只读空态，不进入文档列表、右键菜单和标签详情链路 |

#### 本批新增/修改的关键文件

- `apps/web/src/features/library/__tests__/LibrarySettingsStateMigration.test.tsx`
- `apps/web/src/features/library/LibraryPage.tsx`
- `apps/web/src/features/library/useLibraryState.ts`
- `apps/web/src/features/settings/SettingsPage.tsx`
- `apps/web/src/features/library/__tests__/mockLibraryApi.ts`
- `apps/web/src/i18n.ts`
- `packages/shared/src/library-types.ts`
- `apps/server/src/library/library-config-service.ts`
- `apps/server/src/storage/library-config-store.ts`
- `apps/server/src/library/library-config-service.test.ts`

#### 已执行验证

```bash
pnpm --filter @x-file/web test -- src/features/library/__tests__/LibrarySettingsStateMigration.test.tsx
pnpm --filter @x-file/server test -- src/library/library-config-service.test.ts
pnpm --filter @x-file/shared build
pnpm --filter @x-file/web typecheck
pnpm --filter @x-file/server typecheck
pnpm --filter @x-file/web test
pnpm --filter @x-file/server test
pnpm --filter @x-file/indexer build
```

结果：全部通过。前端 12 个测试文件、74 个测试通过；后端 33 个测试通过。

### 第 6 批：索引状态、刷新策略、轮询、缓存替换与索引稳定性优化

状态：已完成。

#### 行为项覆盖

| 序号 | 旧测试行号 | 行为规格 | X-File 测试证据 | 功能补全 |
|---:|---:|---|---|---|
| 1 | 3745 | 索引状态指示灯悬浮后会显示索引器状态详情 | `apps/web/src/features/library/__tests__/LibraryIndexMigration.test.tsx` | `LibraryIndexStatusPopover` 显示当前状态、时间、dirty reason、错误摘要 |
| 2 | 3773 | 索引状态指示灯会显示当前任务阶段 | `LibraryIndexMigration.test.tsx` | 状态详情消费 `runningStage` 并展示阶段文案 |
| 3 | 3828 | 索引状态指示灯会显示细分导出阶段 | `LibraryIndexMigration.test.tsx` | 增加 `export_search` 等阶段文案映射 |
| 4 | 3856 | 技术详情超出可视区域时会限制高度并允许内部滚动 | `LibraryIndexMigration.test.tsx` | 技术详情区域设置 `maxHeight` 与内部滚动 |
| 5 | 3947 | 点击刷新按钮会手动请求文档库刷新 | `LibraryIndexMigration.test.tsx` | 手动刷新 reason 对齐为 `manual_refresh`，保留 folder targetPath |
| 6 | 3976 | 接口返回新列表后会替换缓存旧列表，已删除文件不会继续显示 | `LibraryIndexMigration.test.tsx` | `reloadDocuments(true)` 继续用新列表替换旧缓存 |
| 7 | 5416 | 进入文档库视图时，不会仅因为快照较旧就自动发起刷新 | `LibraryIndexMigration.test.tsx` | 保持初始进入只读快照与列表，不按旧 `lastCompletedAt` 自动刷新 |
| 8 | 5470 | 索引运行中会自动轮询状态并在完成后刷新显示 | `LibraryIndexMigration.test.tsx` | 轮询 reload snapshot 后在 folder 模式重拉当前列表 |
| 9 | 5548 | 标签筛选浏览时，状态刷新不会因为 lastCompletedAt 变化而自动重拉当前列表 | `apps/web/src/features/library/__tests__/LibraryPage.test.tsx` | `useLibraryState` 文档列表 effect 依赖不包含 `lastCompletedAt` |
| 10 | 5619 | 目录模式下即使索引还在 running，也会主动重拉当前目录列表 | `LibraryIndexMigration.test.tsx` | running/stale/queued 轮询时 folder 模式主动 `reloadDocuments(true)` |
| 11 | 5744 | 默认支持后缀保持原样保存时，仍然提交空白名单让索引器走默认支持范围 | `apps/web/src/features/settings/__tests__/SettingsPage.test.tsx`、`apps/server/src/library/library-config-service.test.ts` | Settings 保存默认范围时提交 `allowedExtensions: []`，服务端保留空白名单语义 |

#### 关联后端稳定性补强

| 能力 | 测试证据 | 说明 |
|---|---|---|
| 队列去重 | `apps/server/src/tasks/task-manager.test.ts` | 同 `taskType/key` 只保留一个任务 |
| queue timeout | `apps/server/src/tasks/task-manager.test.ts` | 排队超时进入 `queue_timeout` 并释放 key |
| run timeout / abort | `apps/server/src/tasks/task-manager.test.ts` | 运行超时触发 abort，失败后释放 key |
| cooldown -> fresh | `apps/server/src/library/index-service.test.ts` | 索引完成后短暂 cooldown，到期恢复 fresh |
| 失败恢复 | `apps/server/src/library/index-service.test.ts` | failed 状态不会卡死后续刷新 |
| running 列表只读 | `apps/server/src/library/library-service.test.ts` | 索引 running 时列表读取 export，不阻塞也不触发二次刷新 |
| 大文件跳过 | `apps/server/src/library/library-index-e2e.test.ts` | 超过上限的文件跳过，export 仍可读 |
| dirty scope | `apps/server/src/library/library-index-e2e.test.ts` | 增量索引返回限定 targetPath 的 dirty scope |
| watch quiet window | `apps/server/src/library/watch-service.test.ts` | quiet window 内连续变更合并为一次刷新 |
| 标签重算解耦 | `apps/server/src/library/tag-service.test.ts` | 标签重算进入标签任务，不耦合索引刷新 |

#### 本批新增/修改的关键文件

- `apps/web/src/features/library/__tests__/LibraryIndexMigration.test.tsx`
- `apps/web/src/features/settings/__tests__/SettingsPage.test.tsx`
- `apps/web/src/features/library/LibraryPage.tsx`
- `apps/web/src/features/library/useLibraryState.ts`
- `apps/web/src/i18n.ts`
- `apps/web/src/styles.css`
- `apps/server/src/library/index-service.test.ts`
- `apps/server/src/library/index-service.ts`
- `apps/server/src/library/library-service.test.ts`
- `apps/server/src/tasks/task-manager.test.ts`

#### 已执行验证

```bash
pnpm --filter @x-file/web test
pnpm --filter @x-file/web typecheck
pnpm --filter @x-file/server test
pnpm --filter @x-file/server typecheck
pnpm --filter @x-file/shared build
pnpm --filter @x-file/indexer build
```

结果：全部通过。

### 第 7 批：列表/Finder 视图、文件类型文案、列宽、虚拟滚动与加载更多

状态：已完成。

#### 行为项覆盖

| 序号 | 旧测试行号 | 行为规格 | X-File 测试证据 | 功能补全 |
|---:|---:|---|---|---|
| 1 | 4162 | 列表模式表头会显示文件名大小时间和种类 | `apps/web/src/features/library/__tests__/LibraryFinderMigration.test.tsx` | Finder 表头稳定显示名称、大小、更新时间、类型、创建时间 |
| 2 | 4211 | 列表模式文件夹种类会显示更像 macOS 的文案 | `LibraryFinderMigration.test.tsx` | 文件夹类型列显示 `libraryFinderKindFolder` 文案 |
| 3 | 4228 | 列表模式会把 html json zip mp4 sql 分成更具体的类型文案 | `LibraryFinderMigration.test.tsx` | 新增 Finder 类型文案映射：文本文档、SQL 脚本、HTML 文档、JSON 配置、压缩归档、视频文件等 |
| 4 | 4316 | 文档名称显示真实文件名，不显示摘要标题 | `LibraryFinderMigration.test.tsx`、`apps/web/src/features/library/__tests__/LibraryPage.test.tsx` | Finder 行继续使用 `resolveLibraryDocumentDisplayName()`，优先显示路径真实文件名 |
| 5 | 4354 | 列表模式列宽支持拖拽调整 | `LibraryFinderMigration.test.tsx` | 列宽拖拽状态写入 `finderColumnWidths`，表头与行共用同一列模板 |
| 6 | 4382 | 列表视图滚动后会继续显示后面的文件夹记录，不会只停在前几项 | `LibraryFinderMigration.test.tsx` | Finder 虚拟列表按滚动位置渲染后段目录 |
| 7 | 4432 | 网格视图虚拟滚动高度按总条数估算，不再只按已加载条数计算 | `LibraryFinderMigration.test.tsx` | `VirtualLibraryGrid` 使用 `visibleEntryTotal` / `total` 推导的估算总数计算 spacer 高度 |
| 8 | 4508 | 列表视图虚拟滚动优先使用后端返回的当前目录可见总条数 | `LibraryFinderMigration.test.tsx` | `LibraryState.visibleEntryTotal` 消费 `documentPage.visibleEntryTotal`，Finder spacer 按可见总条数估算 |
| 9 | 4613 | 列表视图接近已加载尾部时会按总条数提前继续加载 | `LibraryFinderMigration.test.tsx` | Finder 滚动时按当前 scrollTop 即时计算虚拟窗口，接近已加载尾部主动 `loadMore()` |
| 10 | 4710 | 列表视图虚拟内容用 top 定位，避免滚动时把 scrollHeight 越撑越大 | `LibraryFinderMigration.test.tsx` | `.affairs-finder-virtual` 继续使用 `top` 定位，不使用 transform 撑高内容 |
| 11 | 4776 | 列表视图点击表头可以切换排序 | `LibraryFinderMigration.test.tsx` | Finder 表头按钮调用 `getNextSortState()` 并更新排序 |
| 12 | 7213 | 网格模式会给 html json zip mp4 文件显示对应徽标和色调 | `LibraryFinderMigration.test.tsx` | 网格卡片消费 `document-visual.ts`，HTML/JSON/ZIP/VIDEO 徽标和 tone 样式被测试固定 |

#### 本批新增/修改的关键文件

- `apps/web/src/features/library/__tests__/LibraryFinderMigration.test.tsx`
- `apps/web/src/features/library/__tests__/LibraryPage.test.tsx`
- `apps/web/src/features/library/LibraryPage.tsx`
- `apps/web/src/features/library/useLibraryState.ts`
- `apps/web/src/i18n.ts`
- `apps/web/src/test/setup.ts`

#### 已执行验证

```bash
pnpm --filter @x-file/web test -- src/features/library/__tests__/LibraryFinderMigration.test.tsx
pnpm --filter @x-file/web test
pnpm --filter @x-file/web typecheck
```

结果：全部通过。

### 第 8 批：标签筛选树、多选过滤、徽标计数、排序、展开记忆与拼音搜索

状态：已完成。

#### 行为项覆盖

| 序号 | 旧测试行号 | 行为规格 | X-File 测试证据 | 功能补全 |
|---:|---:|---|---|---|
| 1 | 4826 | 标签树路径会在面包屑里显示每一级标签名称 | `apps/web/src/features/library/__tests__/LibraryTagTreeMigration.test.tsx` | 标签筛选后面包屑按路径逐级显示标签名 |
| 2 | 4853 | 标签筛选在列表视图可以切到目录视图，按文件实际路径分组显示 | `LibraryTagTreeMigration.test.tsx` | 标签筛选列表保留“文件/目录”切换，目录模式按文档真实路径构建目录节点 |
| 3 | 4984 | 标签支持多选过滤，并且顶部会出现重置按钮 | `LibraryTagTreeMigration.test.tsx` | `selectedTagPaths` 支持跨业务根多选，选中后显示清除筛选按钮 |
| 4 | 5052 | 有标签选中时，标签树只显示还有结果的选项 | `LibraryTagTreeMigration.test.tsx` | 标签树按 `tagFacetCounts` 过滤无结果分支 |
| 5 | 5118 | 组合筛选后，标签树徽标会显示筛选后的匹配数量 | `LibraryTagTreeMigration.test.tsx` | 选中标签后徽标使用 facet count，不再显示原始 documentCount |
| 6 | 5187 | 列表视图下标签筛选后的文档点击只更新选中态，不会重新请求列表 | `LibraryTagTreeMigration.test.tsx` | 文档行点击只更新 `selectedDocumentId`，不触发列表 effect 重拉 |
| 7 | 5246 | 标签树显示手动业务标签，同时继续隐藏来源、主题、状态这类噪音根标签 | `LibraryTagTreeMigration.test.tsx` | `buildLibraryTagTree()` 改为隐藏噪音根，而不是只允许时间/类型根 |
| 8 | 5282 | 时间标签按最新优先，其他标签按更常访问优先 | `LibraryTagTreeMigration.test.tsx` | 时间标签按最近优先，其他标签按数量优先排序 |
| 9 | 5325 | 每层标签默认最多显示 5 个，并记住展开更多状态 | `LibraryTagTreeMigration.test.tsx` | 新增 `expandedMorePaths` 持久化，每层默认 5 个，展开更多后记忆 |
| 10 | 5361 | 时间树里会显示最近7天标签 | `LibraryTagTreeMigration.test.tsx` | 时间树排序和渲染保留“最近7天” |
| 11 | 5373 | 标签树可以用拼音快速查找并定位标签 | `LibraryTagTreeMigration.test.tsx` | 搜索支持简单拼音匹配并强制展开命中分支 |

#### 本批新增/修改的关键文件

- `apps/web/src/features/library/__tests__/LibraryTagTreeMigration.test.tsx`
- `apps/web/src/features/library/LibraryPage.tsx`
- `apps/web/src/features/library/useLibraryState.ts`
- `apps/web/src/i18n.ts`
- `apps/web/src/styles.css`

#### 已执行验证

```bash
pnpm --filter @x-file/web test -- src/features/library/__tests__/LibraryTagTreeMigration.test.tsx
pnpm --filter @x-file/web test
pnpm --filter @x-file/web typecheck
```

结果：全部通过。

### 第 9 批：标签管理、智能规则与全量重算任务

状态：已完成。

#### 行为项覆盖

| 序号 | 旧测试行号 | 行为规格 | X-File 测试证据 | 功能补全 |
|---:|---:|---|---|---|
| 1 | 5968 | 可以按树状结构管理标签 | `apps/web/src/features/library/__tests__/LibraryTagManagerMigration.test.tsx` | 标签管理弹窗支持根标签和子标签创建，左侧按路径层级缩进展示 |
| 2 | 6124 | 标签改名后会主动刷新左侧标签树名称 | `LibraryTagManagerMigration.test.tsx` | 保存标签后重载标签详情、侧栏标签树和当前文档列表 |
| 3 | 6267 | 标签管理模态框支持添加智能规则并保存 | `LibraryTagManagerMigration.test.tsx` | 标签编辑区支持新增智能规则，保存时提交 `smartRules`、`matcher` 与 `priority` |
| 4 | 6352 | 标签管理模态框支持配置按文件夹子树命中的智能规则 | `LibraryTagManagerMigration.test.tsx` | 智能规则类型支持 `document_path_in_folder`，文件夹路径归一化后保存 |
| 5 | 6444 | 标签管理模态框点击标签后直接进入编辑，并在详情区显示文档数量 | `LibraryTagManagerMigration.test.tsx` | 点击标签立即加载编辑表单；详情区新增只读“文档数量”信息 |
| 6 | 6542 | 标签管理模态框可以发起全量标签重算恢复任务 | `LibraryTagManagerMigration.test.tsx` | 恢复按钮调用 `requestLibraryTagRecompute()` 并刷新标签树/列表 |
| 7 | 6554 | 标签管理模态框会显示全量标签重算的当前进度 | `LibraryTagManagerMigration.test.tsx` | 弹窗轮询 `getLibraryTagRecomputeTask()`，显示 running/queued/fresh/failed 状态文案 |

#### 本批新增/修改的关键文件

- `apps/web/src/features/library/__tests__/LibraryTagManagerMigration.test.tsx`
- `apps/web/src/features/library/LibraryPage.tsx`
- `apps/web/src/shared/modal/ModalAtoms.tsx`
- `apps/web/src/i18n.ts`
- `apps/web/src/styles.css`

#### 已执行验证

```bash
pnpm --filter @x-file/web test -- src/features/library/__tests__/LibraryTagManagerMigration.test.tsx
pnpm --filter @x-file/shared build
pnpm --filter @x-file/web test
pnpm --filter @x-file/web typecheck
pnpm --filter @x-file/server test
pnpm --filter @x-file/server typecheck
pnpm --filter @x-file/indexer build
```

结果：全部通过。

### 第 10 批：文档与文件夹标签分配、推荐标签和任务进度入口

状态：已完成。

#### 行为项覆盖

| 序号 | 旧测试行号 | 行为规格 | X-File 测试证据 | 功能补全 |
|---:|---:|---|---|---|
| 1 | 6603 | 文档详情通过输入匹配添加标签，并且不把时间和类型标签当普通标签 | `apps/web/src/features/library/__tests__/LibraryTagAssignmentMigration.test.tsx` | 文档详情“编辑文档标签”统一打开快速分配弹窗；候选标签过滤时间/类型系统根 |
| 2 | 6702 | 文档详情推荐标签收进标签推荐悬浮层，并排除已分配标签 | `LibraryTagAssignmentMigration.test.tsx`、`apps/web/src/features/library/__tests__/LibraryPage.test.tsx` | 推荐区按 score 排序、最多 8 个，排除已分配标签 |
| 3 | 6782 | 文档标签请求提交后会立刻显示右上角进度入口 | `LibraryTagAssignmentMigration.test.tsx` | 保存请求发出前先生成本地乐观标签任务入口，不等待 API resolve |
| 4 | 6838 | 文档详情输入不存在的标签时会直接创建并分配 | `LibraryTagAssignmentMigration.test.tsx` | 未命中已有标签时提交 `createTagPaths`，同时保留 `tagIds` |
| 5 | 6858 | 文件夹详情可以通过输入分配已有标签 | `LibraryTagAssignmentMigration.test.tsx` | 文件夹右键“标签”复用快速分配弹窗，保存时提交 `folderPath` 与 `tagIds` |
| 6 | 6943 | 文件夹标签请求提交后会立刻显示右上角进度入口 | `LibraryTagAssignmentMigration.test.tsx` | 文件夹标签保存同样生成乐观标签任务入口 |
| 7 | 7006 | 右上角标签任务入口可以展开最近一次标签任务记录 | `LibraryTagAssignmentMigration.test.tsx` | 任务入口可展开“最近标签任务”，显示目标路径和状态 |
| 8 | 7125 | 文件夹详情输入不存在的标签时会直接创建并绑定 | `LibraryTagAssignmentMigration.test.tsx` | 文件夹未命中标签时提交 `createTagPaths` 并绑定到当前文件夹 |

#### 本批新增/修改的关键文件

- `apps/web/src/features/library/__tests__/LibraryTagAssignmentMigration.test.tsx`
- `apps/web/src/features/library/LibraryPage.tsx`
- `apps/web/src/i18n.ts`
- `apps/web/src/styles.css`

#### 已执行验证

```bash
pnpm --filter @x-file/web test -- src/features/library/__tests__/LibraryTagAssignmentMigration.test.tsx
pnpm --filter @x-file/web test
pnpm --filter @x-file/web typecheck
pnpm --filter @x-file/server test
pnpm --filter @x-file/server typecheck
pnpm --filter @x-file/shared build
pnpm --filter @x-file/indexer build
```

结果：全部通过。

### 第 11 批：跨批基础合约、DTO、路由、索引器与样式体系

状态：已完成。

#### 覆盖范围

这一批不是单个旧测试行为，而是所有前面批次依赖的底层合约。已通过测试钉死以下关键点：

- `loadRuntimeConfig()` 能兼容旧配置文件，并规范化 `allowedExtensions`、`includedHiddenPaths`、`watchDebounceMs`、`parserTimeoutMs`。
- `FileScanner` 会跳过隐藏目录和 `.ai-index`，并拒绝 rootDir 外的 `targetPath`。
- `LibraryConfigService` / `/api/library/config` 会持久化 `enabled` 到 binding 和配置文件，并同步到快照。
- `apps/web/src/styles.css` 已存在统一 token：`.primary-button`、`.secondary-button`、`.danger-button`、`.button-row`、`--app-button-*`、`--control-height-*`、`--modal-*`。
- `LibraryPage` / `SettingsPage` 已继续消费全局样式体系，没有把局部魔法值塞回组件。

#### 本批新增/修改的关键文件

- `apps/server/src/library/library-foundation-contract.test.ts`
- `packages/indexer/src/scanner/file-scanner.ts`
- `packages/indexer/src/config/load-runtime-config.ts`
- `packages/indexer/src/scanner/file-scanner.test.ts`（规划中，后续可继续补）
- `packages/shared/src/library-types.ts`
- `apps/server/src/library/library-config-service.ts`
- `apps/server/src/storage/library-config-store.ts`
- `apps/web/src/features/settings/SettingsPage.tsx`
- `apps/web/src/features/library/LibraryPage.tsx`

#### 已执行验证

```bash
pnpm --filter @x-file/indexer build
pnpm --filter @x-file/shared build
pnpm --filter @x-file/web typecheck
pnpm --filter @x-file/server typecheck
pnpm --filter @x-file/web test
pnpm --filter @x-file/server test
```

结果：全部通过。前端 12 个测试文件、74 个测试通过；后端 37 个测试通过。

## 未完成批次

以下批次仍需按“先补失败测试，再迁移功能，最后回写本文档”的方式推进：


## 下一步建议

第 11 批已完成。当前迁移测试链路已闭环，后续若继续补，优先补第 11 批里建议的 P0/P1 回归测试文件，进一步固化底层合约。

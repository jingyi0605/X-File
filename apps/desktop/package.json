{
  "name": "@x-file/desktop",
  "version": "0.1.0-dev.7",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && cargo check --manifest-path src-tauri/Cargo.toml",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/dev-shell.ts",
    "release:check": "node ../../scripts/check-desktop-release-secrets.mjs",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "tsx": "^4.19.2"
  }
}
